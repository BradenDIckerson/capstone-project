(If using visual studio code) Open server from index.html by right clicking and selecting "Open with live server"
Open terminal and type in "node server/server.js" to get "menu" and "location" buttons to work.
